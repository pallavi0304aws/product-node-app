pipeline {
    agent any

    stages {
        stage('Install Dependencies') {
            steps {
                bat 'npm install'
            }
        }

        stage('Verify App Starts') {
            steps {
                bat 'node index.js'
            }
        }
    }

    post {
        always {
            echo "Pipeline completed."
        }
    }
}
